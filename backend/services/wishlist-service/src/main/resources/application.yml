server:
  port: 8084

spring:
  application:
    name: wishlist-service

  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_wishlist?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Rushi@1999
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka