server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_seller_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${SELLER_DB_USERNAME:root}
    password: ${SELLER_DB_PASSWORD:rishabh2506}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  instance:
    prefer-ip-address: true
