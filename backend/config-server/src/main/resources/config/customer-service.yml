server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ecommerce_customer_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${CUSTOMER_DB_USERNAME:root}
    password: ${CUSTOMER_DB_PASSWORD:rishabh2506}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: "*"
