server:
  port: 8082

services:
  auth:
    url: http://localhost:8081

eureka:
  instance:
    prefer-ip-address: true

jwt:
  secret: ${JWT_SECRET:ThisIsADevelopmentSecretKeyForJwtSigning123456789}

management:
  endpoints:
    web:
      exposure:
        include: "*"